import { AdminPasswordCard } from "@/components/admin";

export const metadata = { robots: { index: false, follow: false } };

export default function AdminLoginPage() {
  return <AdminPasswordCard />;
}
